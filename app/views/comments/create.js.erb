document.getElementById('comments_<%= @post.id %>').insertAdjacentHTML('beforeend', "<%= j render 'comments/comment', post: @post, comment: @comment %>");
document.getElementById('comment_content_<%= @post.id %>').value = '';
