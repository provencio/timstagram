.feed
  = render 'post', post: @post
.edit-links
  = link_to "Back to feed", posts_path, class: 'btn btn-quiet'
  - if @post.user == current_user
    = link_to "Edit post", edit_post_path(@post), class: 'btn'
